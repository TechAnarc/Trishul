version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    container_name: trishul_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: trishul
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: trishul_redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:
